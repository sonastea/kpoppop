services:
  database:
    container_name: kpopop_pgsql
    image: "postgres:13.4"
    restart: unless-stopped
    env_file:
      - database.env
    expose:
      - "5432"
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data

volumes:
  pgdata: